faster-whisper>=1.0.0
paho-mqtt>=2.0.0
PyYAML>=6.0
requests>=2.31.0
numpy>=1.24.0
piper-tts>=1.2.0    # TTS-Synthese (optional, deaktivierbar via config)
miniaudio>=1.58     # Audio-Dekodierung (MP3, WAV, OGG, FLAC) für Bestätigungstöne
grpcio>=1.81.1      # gRPC-Laufzeit (Server + Client) — Untergrenze an die grpcio-tools-Version gekoppelt, die core/proto/gen_proto.sh zuletzt zur Stub-Generierung benutzt hat (siehe Kommentar dort)
werkzeug>=3.1.0     # Password-Hashing (generate_password_hash in db.py/user_manager.py/grpc_server.py) — bisher nur transitiv über flask installiert, das mit der alten In-Process-WebUI entfallen ist
boto3>=1.34.0       # AWS Polly (TTS, tts.py) — wurde importiert, war aber nie deklariert (funktionierte nur wenn transitiv vorhanden)
amazon-transcribe>=0.6.2  # AWS Transcribe Streaming (STT, stt.py) — nur nötig wenn stt.aws_* konfiguriert ist
